# Expected output locks

See `reproduce_map.tsv` for the endpoint-fix public values and their source files.
Smoke tests under `tests/smoke_tests/` check the highest-risk values: species counts,
screening counts, LASSO AUCs/predictor counts, Fig. 5B permutation values, and
legacy forbidden values.
