# Scripts

The curated public workflow scripts are stored under `../code/` to preserve the
numbered analysis-stage organization used by the manuscript reproducibility
package. This directory is a lightweight entry point for users expecting a
top-level `scripts/` directory.

Start with `../docs/reproduction_guide.md`, `../docs/workflow_overview.md` and
`../docs/reproduce_map.tsv`.
